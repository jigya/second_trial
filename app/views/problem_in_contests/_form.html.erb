<%= simple_form_for(@problem_in_contest) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :problem %>
    <%= f.association :contest %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
